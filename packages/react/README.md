# @ayatsumugi/react

Reserved for the thin, public React adapter around the separately licensed Tsumugi Wasm
core. The package is marked `private` so the scaffold cannot be published accidentally.

Before publication it needs a stable Wasm ABI, SSR behavior, React 18/19 compatibility
tests, and explicit binary license notices.
